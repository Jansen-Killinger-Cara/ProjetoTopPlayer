{
  "name": "topplayers",
  "version": "1.0.0",
  "scripts": {
   "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix FrontEndTopPlayer\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
